{% extends "base.phtml" %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md">
            <h1>{{ title }}</h1>
            {{ content | raw }}
        </div>
    </div>
</div>
{% endblock %}
